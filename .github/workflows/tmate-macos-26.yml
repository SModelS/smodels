name: Live macOS-26 Shell (tmate)

on:
  workflow_dispatch:

jobs:
  shell:
    runs-on: macos-26
    timeout-minutes: 60

    steps:
      - uses: actions/checkout@v4

      - name: Start SSH session
        uses: mxschmitt/action-tmate@v3
        with:
          sudo: false
