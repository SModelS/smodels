name: Interactive macOS-14 Shell

on:
  workflow_dispatch:

jobs:
  debug:
    runs-on: macos-14
    timeout-minutes: 60

    steps:
      - uses: actions/checkout@v4

      - name: Start SSH session
        uses: mxschmitt/action-tmate@v3
