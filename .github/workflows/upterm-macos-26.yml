name: Interactive macOS-26 upterm Shell

on:
  workflow_dispatch:

jobs:
  debug:
    runs-on: macos-26
    timeout-minutes: 60

    steps:
      - uses: actions/checkout@v4

      - name: Start SSH session
        uses: lhotari/action-upterm@v1
        with:
          limit-access-to-actor: true
